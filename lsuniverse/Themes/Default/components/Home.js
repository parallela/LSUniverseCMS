import React from "react";
import { useTranslation } from "react-multi-lang";

const Home = props => {
    const t = useTranslation();

    return (
        <div id="home">
        </div>
    );
};
export default Home;
